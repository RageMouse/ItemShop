@using Logic.Models
@model ItemShop.Models.ShowAllAccountsViewModel

@{
    ViewData["Title"] = "Create account";
}

<h2>Create Account</h2>

<h4>CreateAccounViewModel</h4>
<hr />
<p>
    <a class="btn btn-danger" role="button" aria-pressed="true" asp-action="CreateNewAccount">CREATE NEW</a>
</p>

<input type="text" id="myInput" onkeyup="SearchCharacterName()" placeholder="Search for your character.." title="Type in a character name">
<table id="adminTable" class="table table-hover table-striped">
    <thead class="thead-dark">
    <tr>
        <th>Name</th>
        <th>Specializaton</th>
        <th>Score</th>
        <th>Created On</th>
        <th>Edit</th>
    </tr>
    </thead>
    @*<tbody>
    @foreach (Account account in Model.Accounts)
    {
        <tr>
            <td>@account.Name</td>
            <td>@account.Password</td>
            <td>@account.Gamemaster</td>
            <td>@account.Active</td>
            <td><a asp-action="Edit" asp-controller="Account" asp-route-id="@account.AccountId">EDIT</a></td>
        </tr>
    }
    </tbody>*@
</table>

<div>
    <a asp-action="Index">Back to List</a>
</div>
